# Matches the upstream Dylint v6.0.4 template. Upgrade this and the adapter together.
[toolchain]
channel = "nightly-2026-05-28"
profile = "minimal"
components = ["llvm-tools-preview", "rustc-dev", "rustfmt"]
