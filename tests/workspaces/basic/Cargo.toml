[package]
name = "statement-spacing-fixture"
version = "0.1.0"
edition = "2024"
publish = false

[workspace]

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(dylint_lib, values("statement_spacing"))'] }
